node_modules/
.env
.env.*
!.env.example
# Committed dev-only frontend config (no secrets); provides VITE_API_URL for `vite dev`.
!apps/web/.env.development
dist/
build/
.svelte-kit/
*.log
.DS_Store
/tmp
coverage/
.zerops/

# SSH decoy host key (generated locally on first run, never committed)
apps/decoys/ssh/.hostkey/

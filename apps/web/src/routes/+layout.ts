// Pure client-side single-page app (no SSR / no prerender) — served as static
// files. Suits a live, WebSocket-driven dashboard and is the cheapest to host.
export const ssr = false;
export const prerender = false;

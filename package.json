{
  "name": "chimera",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Chimera — Self-Serve Deception Mesh (Zerops Challenge 2026)",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "prettier --check .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "prettier": "^3.3.3",
    "prettier-plugin-svelte": "^3.2.7",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
